# Git 元数据不参与镜像构建，避免扩大上下文并泄漏仓库历史。
.git
# GitHub 工作流只在宿主 CI 执行，运行镜像不需要读取。
.github
# tmp 保存本地隔离验证产物，不能进入可复现构建上下文。
tmp
# 架构和开发文档不影响二进制编译，排除后减少发送给 Docker daemon 的内容。
docs
# 各层 README 和其他 Markdown 同样不是镜像运行依赖。
*.md
