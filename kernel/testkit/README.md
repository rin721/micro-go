# kernel/testkit

本包提供不参与生产运行的框架测试辅助能力。

## 为什么这样设计

测试经常需要观察状态序列或只编译模块。把这些能力放在独立包中，可以复用公共 API，同时避免在生产 Application 中加入测试开关。

## 工具

- `RecorderObserver` 使用互斥锁记录事件，并返回切片副本。
- `CompileModules` 只执行模块编译，不构造组件或资源。

本包不提供容器查询、实例替换或绕过模块边界的后门。涉及 Runner 的泄漏检查仍由调用方结合 `goleak` 完成。

