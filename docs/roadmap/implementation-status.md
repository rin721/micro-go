# 实现状态

已实现：Module、Registry、Provider、Binding、Export、强类型配置、稳定依赖图、Compile-only、图导出、Application Singleton、事务 Build、完整生命周期、Runner、Observer、结构化错误、Panic 边界、文件监听、最小 Reload、Zap/Slog、System Clock 和 UUID。

明确未实现：自动扫描、运行时 Resolve、字段注入、多作用域、命名与集合注入、实例代际、局部图重建、动态代理、通信 SDK、业务事件总线和分布式治理。

